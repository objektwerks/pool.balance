window {
  title="Pool Balance"
  width=800
  height=600
}
db {
  url = "jdbc:h2:~/.poolbalance/database;AUTO_RECONNECT=TRUE;INIT=runscript from 'classpath:/ddl.sql'"
  user = "sa"
  password = "sa"
}